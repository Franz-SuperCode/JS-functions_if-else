const text = "Sam is good at codingschool";

const bad = text.replace("good", "bad");
const susi = text.replace("Sam", "Susi");
const good = text.replace("codingschool", "school");
console.log(bad);
console.log(good);